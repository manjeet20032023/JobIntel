# Environment Variables for Resume Matching System

# OpenAI API Configuration (REQUIRED for embeddings)
OPENAI_API_KEY=sk-YOUR_ACTUAL_KEY_HERE
OPENAI_EMBEDDINGS_URL=https://api.openai.com/v1/embeddings
OPENAI_EMBEDDINGS_MODEL=text-embedding-ada-002

# MongoDB (from existing setup)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/jobintel

# Redis (from existing setup, required for notifications)
REDIS_URL=redis://localhost:6379

# JWT (from existing setup)
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# SMTP (from existing setup, for email notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=noreply@jobintel.com
SMTP_PASS=your_app_password_here

# Other existing configs...
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000
